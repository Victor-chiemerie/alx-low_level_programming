#include "main.h"
/**
 *_isdigit - checks if an input is a digit
 *
 *Return: 1 if true else returns 0
 */
int _isdigit(int c){
    if(c >= 48 & c <= 58){
        return (1);
    }
    else return (0);
}
