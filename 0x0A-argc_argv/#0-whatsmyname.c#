#include <stdio.h>
/**
 * main - prints the name of the program
 * 
 * Return: always 0
 */
int main(int argc, char* argv[])
{
    printf("%s\n", argv[0]);
    return (0);
}#include <stdio.h>
/**
 * main - prints the name of the program
 * 
 * Return: always 0
 */
int main(int argc, char* argv[])
{
    printf("%s\n", argv[0]);
    return (0);
}