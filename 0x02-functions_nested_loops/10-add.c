#include "main.h"
/**
 * add - adds two values together
 * @x: first parameter to be added
 * @y: second parameter to be added
 * 
 * Return: the summation of the parameters
 */
int add(int x, int y)
{
    int res = x + y;
     return (res);
}
